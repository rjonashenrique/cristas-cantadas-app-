[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Exemplo de redirecionamento para SPA (opcional)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
